import 'package:flutter/material.dart';

class ZS1Page extends StatelessWidget {
  const ZS1Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ZS1展示界面（4.2~4.5版本）',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
